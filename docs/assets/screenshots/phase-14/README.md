# Phase 14 Screenshot Notes

This directory holds the first-pass screenshot paths used by the English manual in `docs/en-manual.md`.

Current strategy:

- Reuse Chinese screenshot assets for the first English pass when possible.
- Keep filenames stable so later localized replacements do not require link changes.
- Treat missing screenshots as non-blocking for this phase.

Expected first-pass image slots:

- `main-workspace-overview.png`
- `first-http-request.png`
- `response-history-replay.png`
- `mcp-workbench-basics.png`
- `stdio-onboarding-fields.png`

If a screenshot is not available yet, keep the path and replace the asset later without changing the manual structure.
