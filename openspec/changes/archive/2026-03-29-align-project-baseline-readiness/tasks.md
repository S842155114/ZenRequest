## 1. Baseline Inventory

- [x] 1.1 Create the canonical desktop baseline summary from the current implemented capability set and existing verification evidence
- [x] 1.2 Classify current findings into shipped capability, documentation drift, readiness gap, or explicit out-of-scope item

## 2. Documentation Alignment

- [x] 2.1 Update `README.md` so runtime, storage, and product positioning match the implemented desktop application
- [x] 2.2 Publish or link the canonical baseline-readiness summary from a repository-facing document contributors will actually read

## 3. OpenSpec Cleanup

- [x] 3.1 Replace placeholder `Purpose` text in stable OpenSpec capability documents covered by the baseline review
- [x] 3.2 Validate that the new `project-baseline-readiness` capability and related artifacts describe the cleanup scope without implying runtime behavior changes

## 4. Readiness Follow-up

- [x] 4.1 Record the frontend bundle-size warning as a tracked readiness gap or spin it into a dedicated follow-up change
- [x] 4.2 Record non-Tauri adapter limitations as an explicit desktop-scope boundary or propose a separate browser-parity change if scope expands
