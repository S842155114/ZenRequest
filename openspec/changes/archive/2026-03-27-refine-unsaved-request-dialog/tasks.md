## 1. Dirty-Close Dialog Presentation

- [x] 1.1 Add a dedicated visual variant for the unsaved-request close confirmation dialog while keeping existing dialog behavior unchanged
- [x] 1.2 Update the unsaved-request close action labels to cancel, don't save, and save-and-close

## 2. Verification

- [x] 2.1 Add or update tests for the dirty-close dialog variant and the request close confirmation flow
- [x] 2.2 Run `pnpm test` and `pnpm build`
